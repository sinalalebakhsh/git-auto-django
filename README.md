# git-auto-django 🚀

Automatic Git commit & push tool for Django projects.

auto push to github tool for django projects.

## Install
```bash
pip install git-auto-django
# OR
pipenv install
```

#### Features

* **Auto commit & push**
* **Semantic versioning**
* **Internet connection detection**
* **Log rotation**
* **Safe git verification**
* **maybe need to run before**



just do this command in Terminal:
```bash
python push_py.py
```

#### Structure
```
git-auto-django/
├── git_auto/
│   ├── __init__.py
│   ├── main.py         
│   └── version.py       ← مدیریت نسخه
├── pyproject.toml
├── README.md
├── LICENSE
└── .gitignore
```
